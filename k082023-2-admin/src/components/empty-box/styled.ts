import styled from 'styled-components';

const EmptyBoxContainer = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  margin-top: 60px;

  svg {
    font-size: 50px;
  }
`;

export default EmptyBoxContainer;
